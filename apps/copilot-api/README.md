# Copilot API

Main entry point for AI queries. Orchestrates calls between the Model Gateway and Agent Swarms.

## Endpoints
- `POST /api/v1/query`: Submit a natural language prompt.
- `GET /health`: Liveness probe.