apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: gateway-mesh
spec:
  hosts:
  - "*"
  gateways:
  - main-gateway
  http:
  - match:
    - uri:
        prefix: /api/v1
    route:
    - destination:
        host: model-gateway
        port:
          number: 8000
