apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: main-ingress
spec:
  rules:
  - host: ai.sovereign.inc
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: copilot-api
            port:
              number: 8000